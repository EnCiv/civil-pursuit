//- Title

input.form-control.synapp-util-url-to-title(
  type              =   'text' 
  name              =   'title'
  placeholder       =   'http://'
  data-ng-model     =   'entry.title'
)

code.text-primary(ng-show='searchingTitle') Searching title for 
  span(ng-bind='entry.title | shorten:100' title='{{entry.title}}')

code.text-muted(ng-bind='entry.url | shorten:100' title='{{entry.url}}')

code.text-warning(ng-show='searchingTitleFailed') Could not resolve title

//- URL

input.entry-url(
  type              =   'hidden'
  name              =   'url'
  data-ng-model     =   'entry.url'
)
