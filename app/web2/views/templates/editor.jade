// Editor
section.editor-component(data-ng-controller='UploadCtrl')
  form(
    novalidate
    data-ng-submit = 'save()')

    .row(data-ng-show='error')
      .alert.alert-warning.alert-dismissible(role='alert')
        strong Warning!
        div(data-ng-bind='error')

    .row
      .col-xs-12
        .box.prefetch
          .item-media-wrapper
            .item-media
              include editor/image
            //- Upload image button

            .upload-button.btn.btn-default.btn-sm(
              ng-file-select  =   "onFileSelect($files)"
              data-multiple   =   "true"
              title           =   "select file"
              onclick         =   "this.value = null"
                ) Upload image

          .pull-right.box-buttons
            button.btn(data-ng-bind='saver')

            div(data-ng-hide='item.description') New

            div.text-muted(data-ng-bind='((item | calculatePromotionPercentage) || 0) + "%"')

            button.btn(disabled) Details

          .item-text
            include editor/subject
          
            .box-description.description
              include editor/description

            include editor/references

