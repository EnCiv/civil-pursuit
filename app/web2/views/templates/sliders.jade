//-
    ENTRY SLIDERS
    =============

//- Entry sliders

// Entry sliders

section(data-ng-init='evaluation.votes[item._id] = {}')

  div(
    data-ng-repeat        =   'criteria in $root.criterias | criteriaFilter:{type:type}' 
    data-ng-show          =   'item.subject'
  )
    
    //- Criteria name and description
    
    h4
      span(
        data-ng-bind      =  'criteria.name'
        data-toggle       =   "tooltip" 
        data-placement    =   "top" 
        title             =   "{{criteria.description}}"
      )

    //- Slider

    input.slider(
      type                =   'text'
      data-item           =   '{{item._id}}'
      data-criteria       =   '{{criteria._id}}'
      data-ng-model       =   'evaluation.votes[item._id][criteria._id]'
      data-slider-tooltip =   'hide'
      data-slider-min     =   '-1'
      data-slider-max     =   '1'
    )
    
    
    //- div(
    //-   data-ng-init        =   'enableSlider($last)'
    //- )
